# =============================================================================
# AI Photo Studio — Dépendances Python
# Projet IA Générative — Mastère Data Scientist — YNOV Campus Lyon
# =============================================================================

# Interface web
streamlit>=1.28.0

# Suppression de fond (modèle U2Net via HuggingFace)
# Installer avec le backend CPU :  pip install "rembg[cpu]"
# Installer avec le backend GPU :  pip install "rembg[gpu]"
rembg[cpu]>=2.0.50

# Traitement d'images
Pillow>=10.0.0

# HuggingFace — Inference API
huggingface-hub>=0.20.0

# Variables d'environnement
python-dotenv>=1.0.0

# Utilitaires numériques
numpy>=1.24.0

# =============================================================================
# Dépendances OPTIONNELLES — Génération en LOCAL (mode diffusers)
# Décommenter si vous avez un GPU NVIDIA avec 6 GB+ de VRAM
# =============================================================================

# diffusers>=0.24.0
# torch>=2.0.0
# transformers>=4.35.0
# accelerate>=0.24.0
